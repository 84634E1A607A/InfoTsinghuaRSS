# =============================================================================
# OAuth Configuration
# =============================================================================

# Enable/disable OAuth authentication (true/false)
# Set to 'false' to allow public access without authentication
OAUTH_ENABLED=true

# GitLab OAuth Configuration
# Get these from your GitLab application settings

# GitLab URL
# - For GitLab.com: https://gitlab.com
# - For Tsinghua GitLab: https://git.tsinghua.edu.cn
# - For self-hosted: https://your-gitlab-instance.com
GITLAB_URL=https://gitlab.com

# GitLab OAuth Application credentials
# Create an application at: https://gitlab.com/-/profile/applications (or your instance)
GITLAB_CLIENT_ID=your_gitlab_client_id_here
GITLAB_CLIENT_SECRET=your_gitlab_client_secret_here

# OAuth redirect URI (must match your GitLab app configuration exactly)
# For local development: http://localhost:8000/auth/callback
# For production: https://your-domain.com/auth/callback
GITLAB_REDIRECT_URI=http://localhost:8000/auth/callback

# Session secret for OAuth state protection
# IMPORTANT: Generate a unique secret for each deployment
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=your_generated_session_secret_here
